apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tasker-chart.fullname" . }}-config
data:
  DB_HOST: {{ .Values.db.host | quote }}
  DB_PORT: {{ .Values.db.port | quote }}
  DB_NAME: {{ .Values.db.name | quote }}
  DB_USER: {{ .Values.db.user | quote }}